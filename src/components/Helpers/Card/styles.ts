import { createUseStyles } from 'react-jss';

const styles = {
  card: {
    boxShadow: '0px 6px 24px 0px rgba(93,62,188,0.04)',
  },
};

const useStyle = createUseStyles(styles);

export default useStyle;
